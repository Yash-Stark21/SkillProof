# Local-development credentials only. They are intentionally low value; never reuse them.
# Both databases live in the same locally installed PostgreSQL 18 cluster.
DATABASE_URL=postgresql+psycopg://skillproof:skillproof@localhost:5432/skillproof
TEST_DATABASE_URL=postgresql+psycopg://skillproof_test:skillproof_test@localhost:5432/skillproof_test
